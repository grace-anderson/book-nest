<div class="container">
  <h2 class="mt-5 mb-3">
    Your Shared Books
  </h2>

  <p>These are the books you have shared.</p>
  {{! add shared books here }}
  {{!-- {{#each this.books}} --}}
  <div class="row">
    <div class="card col-8 col-sm-10 m-2 pointerCursor" data-id="ID">
      <div class="card-body">
        <span class="float-start">TITLE</span>
        <span class="float-end">DATE ADDED</span>
      </div>
    </div>
  </div>
  {{!-- {{/each}} --}}

  <div class="row mt-3 mb-1 align-items-center">
    <div class="col-auto">
      <a
        type="button"
        class="btn btn-custom btn-outline-primary btn-block mt-3 mb-1 w-100"
        href="/share-book"
      >Share a new book to Book Nest</a>
    </div>
  </div>
</div>

{{! below currently not in use }}
{{! <script src="/js/profile.js"></script> }}